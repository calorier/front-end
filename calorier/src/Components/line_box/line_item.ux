<!-- 线框按钮基组件 -->
<template>
  <div class="line-item bottom-border" onclick="clickItem(id)">
    <div class=left-box>
      <image if="{{showIcon}}" class="head-icon" src="{{icon}}"></image>
      <text class="line-title {{warning ? 'line-title-red' : 'line-title-black'}} {{setTextPosition(textPosition)}}">{{title}}</text>
    </div>
    <div class="right-box">
      <text class="line-remark">{{remark}}</text>
      <image class="icon-remark" src="./arrow_right.png" if="showLinkIcon"></image>
    </div>
  </div>
</template>


<script>
export default {
  props: {
    title: {
      type: String,
      default: ''
    },
    remark: {
      type: String,
      default: ''
    },
    showLinkIcon: {
      type: Boolean,
      default: false
    },
    warning: {
      type: Boolean,
      default: false
    },
    textPosition: {
      type: String,
      default: 'left'
    },
    showIcon:{
      type: Boolean,
      default: false
    },
    icon:{
      type:String,
      default: ''
    },
    id: [String, Number]
  },
  data: {
  },
  clickItem(id) {
    this.$emit('clickItem', { id })
  },
  setTextPosition(pos) {
    switch (pos) {
      case 'left':
        return 'line-title-left'
        break
      case 'center':
        return 'line-title-center'
        break
      case 'right':
        return 'line-title-right'
        break
      default:
        break
    }
  }
}
</script>

<style>
.line-box {
  width: 100%;
  flex-direction: column;
  margin-top: 15px;
  background-color: #fff;
  padding-left: 30px;
  padding-right: 0;
  justify-content: space-between;
}
.line-item {
  height: 100px;
  align-items: center;
  justify-content: space-between;
  padding-right: 30px;
  border: 1px solid transparent;
}
.bottom-border {
  border-bottom-color: #e6e6e6;
}
.line-title {
  font-size: 30px;
}
.right-box {
  align-items: center;
}
.line-remark {
  color: #adadad;
  font-size: 25px;
}
.icon-remark {
  margin-left: 20px;
  width: 18px;
}

.line-title-red {
  color: #ff0000;
}

.line-title-black {
  color: #696969;
}

.line-title-center {
  text-align: center;
  width: 100%;
}

.line-title-left {
  text-align: left;
}

.line-title-right {
  text-align: right;
}

.left-box{
  flex-direction: row;
  justify-content: flex-start;
  align-items: center;
}

.head-icon{
  width: 75px;
  height: 75px;
  margin-right: 25px;
}
</style>