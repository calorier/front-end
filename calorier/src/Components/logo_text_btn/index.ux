<!-- 带有logo的按钮组件 -->

<template>
    <div class="container {{size}} {{type}}" onclick="clickBtn">
        <!-- 按钮图标 -->
        <image class="img {{icon}}" if="{{showIcon}}"></image>
        <!-- 按钮名称 -->
        <text class="title">{{text}}</text>
    </div>
</template>

<script>
export default {
    props: {
        // 按钮大小属性[long|middle|short]
        size: {
            type: String,
            default: 'middle'
        },
        //图标路径url
        icon: {
            type: String,
            default: 'none'
        },
        //按钮文本
        text: {
            type: String,
        },
        //按钮类型（因本地文件不易加载暂时使用type属性类确定图标样式）
        type: {
            type: String,
            default: ''
        },
        //是否显示按钮图标
        showIcon: {
            type: Boolean,
            default: true
        }
    },
    //点击按钮事件
    clickBtn() {
        this.$emit('clickBtn')
    }
}
</script>

<style>
.container {
  flex-direction: row;
  align-items:center;
  height: 86px;
  font-size: 36px;
  padding: 18px 0;
}

.long {
  width: 100%;
  justify-content: flex-start;
}

.middle {
  width: 50%;
  justify-content: flex-start;;
}

.short {
  width: 33%;
  justify-content: flex-start;
}

.img {
  width: 50px;
  height: 50px;
  margin: 0 10px;
}

.display {
  background-image: url("../../../Common/images/icon-display.png");
}

.sort {
  background-image: url("../../../Common/images/icon-sort.png");
}

.date {
  background-image: url("../../../Common/images/icon-date.png");
}

.choose {
  background-image: url("../../../Common/images/icon-choose.png");
}

.title {
  height: 50px;
  margin: 0 10px;
}

.border {
  border: 2px solid #a9a9a9;
  border-radius: 2px;
}

.line {
  border-bottom: 2px solid darkgray;
}
</style>
